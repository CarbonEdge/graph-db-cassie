# Required
LLM_API_KEY=sk-ant-...

# Optional — override defaults
# LLM_PROVIDER=anthropic              # anthropic | openai
# LLM_MODEL=claude-haiku-4-5-20251001 # any model supported by the provider
# CASSIE_URL=http://cassie:8080        # cassie-api URL (change if running externally)
# CASSIE_AI_API_KEY=                   # bearer token to protect /query (empty = open)
